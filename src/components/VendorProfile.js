import React from 'react';

function VendorProfile() {
    return (
        <div className="vendor-profile">
            <h2>Vendor Profile</h2>
            {/* Dynamic vendor profile based on selected vendor will be displayed here */}
        </div>
    );
}

export default VendorProfile;
